/**
 * Created by kalengo on 15/5/6.
 */
function count(cb){
  console.log('constructor');
  gm = require('../../../api/lib/gm').gm

  gm.user.find({}, function(err, result){
    console.log('length:', result.length);
    cb(err, result.length);
  });
}
//not working
//count.prototype.getNumber= function(cb){
//  gm = require('../../../api/lib/gm').gm
//
//  gm.user.find({}, function(err, result){
//    cb(err, result.length);
//  });
//}
exports.count = count;
